<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="News_aggregator.Pages.ResoursePage"
             Title="Resourses" 
             BackgroundColor="#212121">
    <CollectionView x:Name="collectionView" Margin="10" SelectionMode="Multiple" SelectionChanged="OnSelectionChenged" >
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <StackLayout  Orientation="Horizontal">
                    <CheckBox x:Name="{Binding NameItem}" Color="#673AB7" IsChecked="{Binding isChecked}" CheckedChanged="CheckedCheckBox"/>
                    <Label Text="{Binding  Text}" FontSize="15" TextColor="White"/>
                </StackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>