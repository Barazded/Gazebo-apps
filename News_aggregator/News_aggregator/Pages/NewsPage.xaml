<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="News_aggregator.Pages.NewsPage"
             Title="News">
    
    <StackLayout>
        <CollectionView x:Name="collectionResourses" Margin="10">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Orientation="Vertical">
                        <Label Text="{Binding Title}" FontSize="20" TextColor="White" Padding="0,10"/>
                        <Label Text="{Binding Info}" FontSize="20" TextColor="White" Padding="0,10"/>
                        <Label Text="{Binding Date}" FontSize="20" TextColor="White" Padding="0,10"/>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>